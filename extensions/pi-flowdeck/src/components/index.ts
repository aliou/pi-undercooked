import type { ExtensionAPI } from "@mariozechner/pi-coding-agent";

export function registerRenderers(_pi: ExtensionAPI) {
  // Register your custom renderers here
  // Example:
  // pi.registerRenderer("myTool", myToolRenderer);
}
