import type { ExtensionAPI } from "@mariozechner/pi-coding-agent";

export function registerProviders(_pi: ExtensionAPI) {
  // Register your custom LLM providers here
  // Example:
  // pi.registerProvider("my-provider", { baseUrl: "...", apiKey: "..." });
}
